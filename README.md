# Musxi Player

使用 C++17、Win32、GDI+ 和 Windows MCI 实现的音乐播放器，适用于 Windows 10/11 x64。默认白色界面搭配绿色强调色，右上角换肤图标按浅色 → 深色 → 透明 → 浅色循环切换；透明主题采用偏黑的深色半透明配色，窗口不透明度约 67%；空白区域不支持点击穿透，顶部空白区域可拖动窗口，无额外拖动条，包含发现搜索、音乐库、圆形账号头像和底部播放控制区。C++ 主程序无需 Qt 或额外的 C++ 运行库，酷狗连接通过随包提供的本机 Node.js 进程实现。

## 酷狗概念版（新增）

1. 启动 `build/MusxiPlayer.exe`，点击左侧「音乐库」或圆形头像。
2. 点击「扫码登录」，使用手机酷狗概念版扫描播放器里的二维码，并在手机上确认。
3. 登录成功后自动读取账号的歌单，包括「我喜欢」和收藏歌单；点击歌单加载其中的歌曲。
4. 双击歌曲，获取当前账号可用的音频地址，缓存在本机后播放。底部播放、暂停、切歌和进度条继续可用。
5. 在官方 App 修改收藏后，点击「同步歌单」刷新。退出账号会停止云端播放并清除本机保存的登录凭证。

此版本使用 KuGouMusicApi 第三方接口，概念版配置为 `platform=lite`，并非官方开放平台授权。仅通过返回给当前账号的播放地址读取音频；会员、下架、风控或接口变更可能影响部分歌曲及登录。需要额外安全验证时，请在官方客户端处理后重试。

歌单同步仍从酷狗读取。登录后可右键搜索结果或歌单内歌曲，选择「添加到歌单」或「收藏到“我喜欢” / 取消收藏」，这些操作会修改酷狗账号中的对应歌单。仅列出可编辑歌单，添加前检查重复，操作后重新读取确认结果；取消收藏只从系统“我喜欢”移除。登录二维码约三分钟后停止轮询，过期后点击「扫码登录」重新生成。本次没有加入网易云音乐接入。

登录凭证使用 Windows DPAPI 加密，保存在 `%LOCALAPPDATA%\MintPlayer\kugou-session.dat`。歌单快照保存在同目录 `kugou-library.json`，用于记录最近同步结果，当前没有离线歌单浏览模式。音频临时文件位于 `cloud-cache` 下的进程目录，正常切歌或退出时删除当前播放缓存。强制结束程序时可能留下缓存，可在关闭播放器后手动清理该目录。

C++ 与接口子进程通过匿名管道通信，不开放 HTTP 端口；接口进程不记录账号凭证日志。账号登录态与设备标识仅在该进程内使用，凭证不会出现在启动命令行中。

### 安装云端依赖与便携打包

当前交付目录已配置好运行环境，可直接启动。重新从源码部署时先安装 Node.js LTS，再执行：

```powershell
./setup-cloud.ps1
./build.ps1
./setup-cloud.ps1 -Package -SkipInstall
```

分发时需同时保留 `build/MusxiPlayer.exe`、`build/runtime` 和 `build/services`，不能只复制 EXE。程序支持 `--kugou` 参数，启动后直接进入云端登录页。第三方源码版本和许可信息见 `THIRD_PARTY_NOTICES.md`。

## 直接使用

双击 `build/MusxiPlayer.exe` 启动。UI 延伸至整个窗口，右上角为自绘最小化、最大化 / 还原和关闭按钮；顶部空白区域可拖动或双击最大化，边缘可调整窗口大小。

1. 在「发现」页输入歌曲或歌手名称，按 Enter 或点击「搜索」。未登录也可搜索，每页显示 30 首，滚轮浏览，底部切换分页。
2. 点击圆形头像或「音乐库」扫码登录。登录后左上角只显示圆形头像，未登录显示空白圆形占位。
3. 双击搜索结果或歌单歌曲播放。底部按钮支持暂停、恢复、上一首和下一首；跨页搜索不改变正在播放的队列。
4. 全宽进度条位于播放栏顶部，悬停时平滑变粗并显示拖动点，移开后恢复细线。悬停气泡显示指针对应的歌曲时间；点击或拖拽松开后跳转，拖动期间预览目标进度。播放结束自动切换下一首并循环队列。

已移除本地导入按钮、Ctrl+O 和拖拽导入，也不再恢复旧的本地歌曲列表。旧音频文件不受影响。搜索播放使用当前账号可用音源。

## 快捷键

| 按键 | 操作 |
| --- | --- |
| 输入框内 Enter | 搜索 |
| 列表内 Enter | 播放选中歌曲 |
| 空格 | 播放 / 暂停（输入框内正常输入空格） |
| ← / → | 上一首 / 下一首（输入框内移动光标） |
| ↑ / ↓ | 选择歌曲 |

## 编译

已有 MinGW-w64 时，在 PowerShell 中运行：

```powershell
./build.ps1
```

脚本优先使用 PATH 中的 `g++.exe`，也支持本机 `D:\msys64\ucrt64\bin\g++.exe`。

也提供 CMake 配置供安装 Visual Studio C++ 工具链的环境使用：

```powershell
cmake -S . -B build-vs
cmake --build build-vs --config Release
```

## 验证

```powershell
./build.ps1 -Test
```

测试生成无声 WAV 文件，通过真实 Windows MCI 验证中文路径导入、去重、播放推进、暂停、恢复、进度跳转、切歌、列表循环、播放完成后的自动切换及损坏文件处理。测试不会修改用户保存的歌曲列表。界面预览输出到 `build/preview-*.png`，其中歌曲为测试文件。

本机已使用 MinGW-w64 完成编译和上述 WAV 集成测试。MP3/WMA 使用系统解码器，实际兼容性取决于文件编码和 Windows 解码支持；未逐一验证所有编码变体。暂不支持 FLAC 和歌词。已支持云端歌单封面、歌曲列表封面和播放栏封面；仅加载当前可见封面，在内存中缓存，缺失或下载失败时显示默认图标。

云端逻辑测试：`node --test services/bridge.test.cjs services/song-actions.test.cjs services/vip.test.cjs`。覆盖扫码状态、设备标识一致性、分页去重、部分同步失败、过期登录、播放权限和错误信息脱敏。原生联调程序 `tests/cloud-live.cpp` 验证子进程通信、真实酷狗二维码生成、GDI+ 解码和扫码状态查询。已验证真实账号收藏同步、搜索和头像获取。`preview-kugou-library.png` 与 `preview-kugou-tracks.png` 中展示的是明确标注的测试数据。

后续修复：兼容酷狗在分页末尾返回 `info: null`，避免将正常空页误报为数据结构变化。已通过真实已登录账号的完整歌单及歌曲列表同步验证；在线播放仍未完成真实账号验证。`tests/cloud-diagnose.cpp --verify-sync` 对应诊断可执行文件的 `--verify-sync` 模式，只输出同步数量，不输出账号凭证或歌曲内容。

## 文件

- `src/main.cpp`：界面绘制、窗口交互、音频控制。
- `tests/smoke.cpp`：原生音频集成测试与界面渲染检查。
- `build.ps1`：MinGW-w64 编译和测试脚本。
- `CMakeLists.txt`：CMake 工程配置。

歌曲右键操作已通过模拟写入、去重、收藏取消、权限拒绝及失败处理测试；真实账号验证覆盖菜单读取、可编辑歌单和收藏状态，未通过测试修改现有收藏。

## 登录自动领取每日概念版 VIP

扫码登录或启动恢复登录后，先查询普通及概念版专属 VIP 状态；已是 VIP 时静默跳过。非 VIP 会检查当天领取记录，未领取时申请服务器当前日期的每日权益。仅在领取记录及 VIP 生效状态均验证成功时提示“VIP 领取成功”，已领取不提示。接口失败、资格不满足或结果不明时不显示成功提示，仍继续同步歌单。同次运行中同账号同日不重复提交领取。

使用现有每日权益领取接口，实际资格由酷狗服务端决定。已通过领取成功、已领取、已有概念版 VIP、未生效和失败分支的模拟测试；真实账号已验证为已有概念版 VIP，自动检测正确静默跳过，未进行新增领取。
“我喜欢”默认按收藏时间从新到旧显示；时间相同保持原相对顺序，无时间信息的歌曲排在后面。其他歌单保持接口原顺序。右侧滚动条支持悬停变亮和变宽、离开后淡出、滑块平滑移动及拖动定位；滑块最小长度增加，仍可定位到列表末尾。
播放栏下一首按钮右侧新增音量控件：点击或拖动音量条调整 0–100%，点击扬声器静音，再次点击恢复静音前音量。默认 75%，同次运行切歌沿用音量；只调整播放器进程的音频会话，不修改系统总音量。
